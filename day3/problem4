#function for palindrome 

def is_palindrome(text) :
    rev_text = text[::-1]
    if(text == rev_text) :
        print(f"'{text}' is a palindrome")
    else :
        print(f"'{text}' is not a palindrome")
text = input("Enter your word or phase here :\n")

is_palindrome(text)
