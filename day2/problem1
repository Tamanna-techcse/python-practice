
numbers = list(map(int , input().split()))

print("The list of numbers is :\n", numbers)
    # print("Largest number :" , max(numbers))
    # print("Smallet number :" , min(numbers))

    # numbers.sort()
    # print("The largest no.is :", numbers[-1])
    # print("The smallest no. is :", numbers[0])

max_value = numbers[0]

for x in numbers :
    if x > max_value :
        max_value = x
print(max_value)
    #print("lage is :", large)

min_value = numbers[0]
for x in numbers :
    if x < min_value :
        min_value = x
print(min_value)
