# determine whether a number is even or odd
while True :
    try:
        num = float(input("Enter a number below !\n"))
        if(num % 2 == 0) :
            print(f"{num} is an even number.")
        else :
            print(f"{num} is an odd number.")
    except ValueError :
        print("Wrong input !!\nPlease provide a valid input")